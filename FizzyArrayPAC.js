
numarray(7)
numarray(3)

resultArray = numarray(10) ;

function numarray(n) {
    resultArray = Array[n]
    for (w = 0; w < n; w++) {
        resultArray[w] === w ;
    return resultArray;
    }
    console.log(resultArray);
}